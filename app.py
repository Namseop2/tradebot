import streamlit as st

# 모바일 화면에 최적화된 설정
st.set_page_config(page_title="우리집 주식 비서", layout="centered")

st.title("🏠 2026 부부 통합 대시보드")
st.write("오후 3시, 데이터에 기반한 의사결정을 돕습니다.")

# 사이드바에서 사용자 선택
user = st.sidebar.selectbox("👤 누구의 포트폴리오를 볼까요?", ["나 (본인)", "와이프"])

st.divider()

if user == "나 (본인)":
    st.subheader("👔 내 보유 종목")
    st.info("현재 현대차, POSCO홀딩스 감시 중 (테스트)")
    # 나중에 여기에 Z-Score와 ATR 로직이 들어갑니다.
else:
    st.subheader("💍 와이프 보유 종목")
    st.success("현재 한국항공우주 감시 중 (테스트)")

st.sidebar.write("---")
st.sidebar.button("🔄 데이터 새로고침")